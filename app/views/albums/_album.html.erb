<li>
<span class="content"><%= album.name %></span>
<span class="content"><%= album.description %></span>
<span class="timestamp">
Created <%= time_ago_in_words(album.created_at) %> ago.
</span>
<% if current_user?(album.user) %>
<%= link_to "delete", album, method: :delete,
data: { confirm: "You sure?" },
title: album.name %>
<% end %>

</li>
